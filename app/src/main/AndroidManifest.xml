<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthhawk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthHawk">
        <activity
            android:name=".Timer"
            android:exported="true" />
        <activity
            android:name=".DisplayWorkoutInfo"
            android:exported="true"
            android:label="@string/workout_info"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity
            android:name=".Intervals"
            android:exported="true" />
        <activity
            android:name=".AddWorkout"
            android:exported="true"
            android:label="@string/add_your_workout_title"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity
            android:name=".Workouts"
            android:exported="true" />
        <activity android:name=".LoginActivity">
        <activity android:name=".FoodAndCaloriesFunctionality">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity">

        </activity>

        <activity android:name=".RegisterActivity">

        </activity>
    </application>

</manifest>